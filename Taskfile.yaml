version: 3

includes:
  shared:
    taskfile: hack/common/Taskfile_root.yaml
    flatten: true
    vars:
      CODE_DIRS: '{{.ROOT_DIR}}/cmd/... {{.ROOT_DIR}}/pkg/...'
      NAME: 'kw_garden'
      REPO_NAME: 'https://github.com/Diaphteiros/kw_garden'
      GENERATE_DOCS_INDEX: "true"
